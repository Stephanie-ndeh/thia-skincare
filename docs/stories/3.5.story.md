# Story 3.5 — Checkout Page (Frontend)

## Status
done

## Description
Build the multi-step checkout page where authenticated users enter delivery details, select a mobile money payment channel, review their order summary, and submit the order. Shipping cost is calculated live as the user types their city.

## Acceptance Criteria
- [x] Page at `pages/checkout.vue` with `definePageMeta({ layout: 'checkout', ssr: false })`
- [x] Unauthenticated users are redirected to `/auth/login?redirect=/checkout` by the `auth` middleware
- [x] `DeliveryForm.vue` collects: `full_name`, `phone`, `city`, `address`, `region`
- [x] Phone field validated with `isValidCameroonPhone()` from `@thia/shared` on blur; inline error shown if invalid
- [x] Region field is a dropdown populated from `CAMEROON_REGIONS` constant from `@thia/shared`
- [x] City field change triggers a debounced (500ms) call to `POST /shipping/calculate`; shipping rate updates reactively
- [x] `PaymentMethodSelector.vue` displays MTN Mobile Money (`cm.mtn`) and Orange Money (`cm.orange`) as styled radio cards
- [x] `CheckoutOrderSummary.vue` shows cart items, subtotal, shipping, discount (if any), and total — all via `formatXAF()`
- [x] "Place Order" button disabled while any required field is empty or phone is invalid
- [x] Place Order flow: validate form → `POST /orders` → `POST /payments/initialize` → redirect to `payment_url`
- [x] Loading state shown on Place Order button during API calls
- [x] Error state shown if either API call fails (message displayed above button)

## Technical Notes

**Components to create**:

`components/checkout/DeliveryForm.vue`
- Props: `modelValue: DeliveryData` (emit updates via `update:modelValue`)
- `DeliveryData` interface: `{ fullName: string; phone: string; city: string; address: string; region: string }`
- Phone validation: `isValidCameroonPhone(phone)` on blur — show red error text if false
- City input: `watch(city, debounce(() => emit('city-changed', city), 500))`
- Region: `<select>` with options from `CAMEROON_REGIONS`

`components/checkout/PaymentMethodSelector.vue`
- Props: `modelValue: 'cm.mtn' | 'cm.orange'`
- Two styled radio cards; selected card has highlighted border
- Emit `update:modelValue` on selection

`components/checkout/CheckoutOrderSummary.vue`
- Props: `{ items: CartItemWithProduct[], subtotal: number, shippingRate: number, discountAmount: number, discountCode: string | null, total: number }`
- All prices rendered with `formatXAF()`
- Shipping row shows "Free" when rate is 0

**Place Order flow** (in `pages/checkout.vue`):
```ts
const order = await $fetch(`${apiBase}/orders`, { method: 'POST', body: orderPayload })
const payment = await $fetch(`${apiBase}/payments/initialize`, {
  method: 'POST',
  body: { orderId: order.data.id, channel: selectedChannel, ... }
})
await navigateTo(payment.data.payment_url, { external: true })
```

## Dependencies
- Story 3.1 (Shipping) — `POST /shipping/calculate` must exist
- Story 3.3 (Order Creation) — `POST /orders` must exist
- Story 3.4 (Payments) — `POST /payments/initialize` must exist
- Story 3.7 (Frontend API Integration) — `useCheckout` composable defined there

## Definition of Done
- [x] All acceptance criteria met
- [x] Zero TypeScript errors
- [x] No `any` types
- [x] Phone validation blocks submission for invalid numbers
- [x] Shipping rate updates automatically when city changes
- [x] All three child components render correctly with props
- [x] Successful flow redirects to NotchPay payment page
- [ ] Reviewed by QA

## Dev Agent Record

### File List
- `apps/frontend/components/checkout/DeliveryForm.vue` — created, v-model with DeliveryData, phone blur validation, debounced city-changed emit via @vueuse/core
- `apps/frontend/components/checkout/PaymentMethodSelector.vue` — created, two styled radio cards for cm.mtn / cm.orange
- `apps/frontend/components/checkout/CheckoutOrderSummary.vue` — created, items list + totals with formatXAF, free shipping label
- `apps/frontend/pages/checkout.vue` — created, auth middleware, inline place-order flow (POST /orders then POST /payments/initialize), disabled button when form invalid

### Completion Notes
Place Order flow is inline in checkout.vue as specified; story 3.7 will extract it to useCheckout composable. City debounce uses useDebounceFn from @vueuse/core (already in deps). Auth token fetched inline from supabaseClient.auth.getSession(). TypeScript check passes with exit 0.
