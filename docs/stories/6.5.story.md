# Story 6.5 — Static Pages (About, FAQ, Contact)

## Status
ready-for-dev

## Description
Complete the informational pages: About, FAQ, and Contact. The Contact page submits to a new backend endpoint that sends an email to the admin via Resend. AppFooter and AppHeader are updated with links to these pages.

## Acceptance Criteria
- [ ] `pages/about.vue` exists with brand story section, "Why Thia?" section, team/founder placeholder, values (Natural, Effective, Affordable), and CTA button to `/categories`
- [ ] `pages/faq.vue` exists using shadcn Accordion with all 8 required questions answered; contact CTA at the bottom
- [ ] `pages/contact.vue` exists with name/email/phone/message form; on submit calls `POST /contact`; displays phone numbers +237 670 076 224 and +237 676 328 226; shows business hours; includes WhatsApp button linking to `wa.me/237670076224`
- [ ] `POST /contact` backend endpoint implemented (no auth required); accepts `{ name, email, phone, message }`; sends email to admin via Resend; returns `{ success: true }`
- [ ] `AppFooter.vue` links to About, FAQ, and Contact; social media link placeholders added; phone numbers verified correct
- [ ] `AppHeader.vue` navigation updated (consider adding About or FAQ link)
- [ ] Contact form shows success/error feedback after submission

## Technical Notes
- FAQ questions to include:
  1. "How long does delivery take?"
  2. "Which areas do you deliver to?"
  3. "How do I pay with Mobile Money?"
  4. "Can I return a product?"
  5. "Are your products natural/organic?"
  6. "How do I track my order?"
  7. "Do you offer bulk discounts?"
  8. "How do I contact customer support?"
- shadcn Accordion: use `<Accordion type="single" collapsible>` with `<AccordionItem>`, `<AccordionTrigger>`, `<AccordionContent>`
- Backend `POST /contact` route lives in `apps/backend/src/routes/contact.ts` and is registered in `app.ts`
- Resend SDK already used in the project — follow existing email service pattern
- Contact form phone field: `inputmode="tel"` for mobile numeric keyboard
- WhatsApp URL format: `https://wa.me/237670076224` (no `+` in wa.me links)

## Dependencies
None — this story can be started independently.

## Definition of Done
- [ ] All acceptance criteria met
- [ ] Zero TypeScript errors
- [ ] No `any` types
- [ ] Reviewed by QA
