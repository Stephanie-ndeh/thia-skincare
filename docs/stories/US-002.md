# Story 1.2: Nuxt 3 Frontend Scaffold

Status: ready-for-dev

## Story

As a developer,
I want the Nuxt 3 frontend application scaffolded with TypeScript, Tailwind CSS v4, shadcn-vue, Pinia, and @nuxtjs/supabase configured,
so that the frontend is ready for feature development with the correct rendering strategy and design system foundation.

## Acceptance Criteria

1. Nuxt 3 project initialized at `apps/frontend/` with TypeScript enabled and SSR on
2. Tailwind CSS v4 installed and configured with brand token placeholders in `assets/css/main.css`
3. shadcn-vue initialized with default configuration
4. Pinia installed and registered as a Nuxt module
5. @nuxtjs/supabase installed and configured (reads env vars from root)
6. `nuxt.config.ts` includes hybrid rendering route rules per architecture doc Section 3.1
7. Complete directory structure created per architecture doc Section 2 (components/, composables/, layouts/, pages/, stores/, middleware/, plugins/, types/, utils/)
8. `@thia/shared` workspace dependency added to `package.json`
9. `npm run dev` starts the dev server without errors
10. `npm run build` produces a production build without errors

## Tasks / Subtasks

- [ ] Task 1: Initialize Nuxt 3 project (AC: #1)
  - [ ] Run `npx nuxi@latest init apps/frontend` or create manually
  - [ ] Configure `nuxt.config.ts` with `ssr: true`, `typescript: { strict: true }`
  - [ ] Set `devtools: { enabled: true }` for development
- [ ] Task 2: Install and configure Tailwind CSS v4 (AC: #2)
  - [ ] Install `@nuxtjs/tailwindcss` and `tailwindcss` v4
  - [ ] Create `assets/css/main.css` with Tailwind directives and `@theme` block
  - [ ] Add brand color placeholders: `--color-brand-primary`, `--color-brand-secondary`, `--color-brand-accent`, `--color-brand-dark`, `--color-brand-light`
  - [ ] Add font placeholders: `--font-heading`, `--font-body`
  - [ ] Add semantic tokens: `--color-surface`, `--color-text-primary`, `--color-text-muted`
- [ ] Task 3: Install and configure shadcn-vue (AC: #3)
  - [ ] Install `shadcn-nuxt` module
  - [ ] Run `npx shadcn-vue@latest init` — configure with TypeScript, Tailwind CSS
  - [ ] Verify `components/ui/` directory created
- [ ] Task 4: Install and configure Pinia (AC: #4)
  - [ ] Install `@pinia/nuxt` and `pinia`
  - [ ] Add `@pinia/nuxt` to `nuxt.config.ts` modules
- [ ] Task 5: Install and configure @nuxtjs/supabase (AC: #5)
  - [ ] Install `@nuxtjs/supabase`
  - [ ] Add to `nuxt.config.ts` modules
  - [ ] Configure to read `SUPABASE_URL` and `SUPABASE_KEY` from environment
  - [ ] Set `supabase: { redirectOptions: { login: '/auth/login', callback: '/auth/confirm' } }`
- [ ] Task 6: Configure hybrid rendering route rules (AC: #6)
  - [ ] Add `routeRules` to `nuxt.config.ts` per architecture doc:
    - `/`: `{ swr: 60 }`
    - `/categories/**`: `{ swr: 60 }`
    - `/products/**`: `{ swr: 30 }`
    - `/testimonials`: `{ swr: 300 }`
    - `/cart`: `{ ssr: false }`
    - `/checkout`: `{ ssr: false }`
    - `/auth/**`: `{ ssr: false }`
    - `/account/**`: `{ ssr: false }`
    - `/admin/**`: `{ ssr: false }`
    - `/order-confirmation/**`: `{ ssr: false }`
- [ ] Task 7: Create directory structure (AC: #7)
  - [ ] Create `components/layout/` with `.gitkeep`
  - [ ] Create `components/product/` with `.gitkeep`
  - [ ] Create `components/cart/` with `.gitkeep`
  - [ ] Create `components/checkout/` with `.gitkeep`
  - [ ] Create `components/review/` with `.gitkeep`
  - [ ] Create `components/testimonial/` with `.gitkeep`
  - [ ] Create `components/admin/` with `.gitkeep`
  - [ ] Create `composables/` with `.gitkeep`
  - [ ] Create `layouts/` directory
  - [ ] Create `middleware/` directory
  - [ ] Create `plugins/` directory
  - [ ] Create `stores/` directory
  - [ ] Create `types/index.ts` with placeholder
  - [ ] Create `utils/formatCurrency.ts` with `formatXAF` function
  - [ ] Create `utils/formatPhone.ts` with placeholder
  - [ ] Create `utils/validators.ts` with placeholder
  - [ ] Create all page directories per architecture doc (empty for now — pages are later stories)
- [ ] Task 8: Add shared package dependency (AC: #8)
  - [ ] Add `"@thia/shared": "workspace:*"` to `apps/frontend/package.json`
- [ ] Task 9: Add runtime config (AC: #6)
  - [ ] Configure `runtimeConfig` in `nuxt.config.ts`:
    - `public.apiBaseUrl`: reads from `NUXT_PUBLIC_API_BASE_URL`
    - `public.siteUrl`: reads from `NUXT_PUBLIC_SITE_URL`
- [ ] Task 10: Verify build (AC: #9, #10)
  - [ ] Run `npm run dev` from `apps/frontend/` — must start without errors
  - [ ] Run `npm run build` from `apps/frontend/` — must produce `.output/` without errors

## Dev Notes

- Tailwind CSS v4 uses the new `@theme` directive instead of `tailwind.config.js` for tokens — architecture doc Section 3.5
- Brand color values are placeholders (comments) until client provides the style guide
- `formatXAF` utility per architecture doc Section 11.4:
  ```typescript
  export function formatXAF(amount: number): string {
    return new Intl.NumberFormat('fr-CM', {
      style: 'currency',
      currency: 'XAF',
      minimumFractionDigits: 0,
      maximumFractionDigits: 0,
    }).format(amount)
  }
  ```
- Do NOT create page content yet — just the directory structure. Pages come in US-007 and US-008
- @nuxtjs/supabase handles session cookies automatically (httpOnly) — no custom plugin needed

### Project Structure Notes

- All frontend code lives under `apps/frontend/`
- shadcn-vue components go in `components/ui/` (auto-generated by CLI)
- Custom domain components go in `components/{domain}/`

### References

- [Source: docs/architecture.md#2. Monorepo Structure]
- [Source: docs/architecture.md#3. Frontend Architecture (Nuxt 3)]
- [Source: docs/architecture.md#3.1 Rendering Strategy]
- [Source: docs/architecture.md#3.5 Design System Integration]
- [Source: docs/architecture.md#11.4 Currency Handling]
- [Source: docs/prd.md#FR-1.1]
- [Source: docs/prd.md#FR-1.11]

## Files to Create

```
apps/frontend/
├── nuxt.config.ts
├── app.vue
├── package.json
├── tsconfig.json
├── assets/
│   └── css/
│       └── main.css
├── components/
│   ├── ui/                    # shadcn-vue (generated)
│   ├── layout/.gitkeep
│   ├── product/.gitkeep
│   ├── cart/.gitkeep
│   ├── checkout/.gitkeep
│   ├── review/.gitkeep
│   ├── testimonial/.gitkeep
│   └── admin/.gitkeep
├── composables/.gitkeep
├── layouts/
├── middleware/
├── pages/
├── plugins/
├── server/
├── stores/
├── types/
│   └── index.ts
└── utils/
    ├── formatCurrency.ts
    ├── formatPhone.ts
    └── validators.ts
```

## Definition of Done

- [ ] `npm run dev` starts Nuxt dev server at localhost:3000
- [ ] `npm run build` succeeds with zero errors
- [ ] Tailwind CSS classes render correctly (test with a simple class in app.vue)
- [ ] Pinia is registered and accessible via `useNuxtApp()`
- [ ] Supabase module loads without errors (connection errors OK — no project yet)
- [ ] Route rules applied (verify via Nuxt DevTools or build output)
- [ ] `@thia/shared` importable from frontend code
- [ ] No TypeScript errors (`npx nuxi typecheck`)

## Dev Agent Record

### Agent Model Used
### Debug Log References
### Completion Notes List
### File List
