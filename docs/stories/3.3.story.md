# Story 3.3 — Order Creation (Backend)

## Status
done

## Description
Implement backend routes to create a new order from cart contents and retrieve order history. Order creation validates stock, computes all monetary values as integers, applies any discount, generates a human-readable reference, and persists both the order header and line items atomically.

## Acceptance Criteria
- [x] `POST /orders` requires authentication (`fastify.authenticate`)
- [x] `POST /orders` validates that every line item has sufficient stock
- [x] All monetary values stored and returned as XAF integers (no decimals)
- [x] Percentage discount uses `Math.floor(subtotal * value / 100)`
- [x] Order reference generated in format `THIA-XXXXXX` (6 random uppercase alphanumeric characters)
- [x] Order row and all `order_items` rows created in a single atomic operation
- [x] `POST /orders` returns `{ data: Order }` with full order and items
- [x] `GET /orders/:id` requires authentication; returns 404 if order belongs to another user
- [x] `GET /orders` requires authentication; returns `PaginatedResponse<Order>` (default page size 10)
- [x] All three routes registered in `src/app.ts`
- [x] `CreateOrderRequest` added to `packages/shared/types/order.ts`

## Technical Notes

**New shared type** — add to `packages/shared/types/order.ts`:
```ts
export interface CreateOrderRequest {
  items: Array<{
    variantId: string
    quantity: number
    unitPrice: number
  }>
  discountCode?: string
  discountAmount?: number
  shippingZoneId?: string
  shippingAmount: number
  deliveryAddress: {
    fullName: string
    phone: string
    city: string
    address: string
    region: string
  }
}
```

**Route file**: `apps/backend/src/routes/orders/index.ts`

**Order creation logic**:
1. Authenticate request → get `userId`
2. For each item: query `product_variants` for `stock_quantity`; if any variant has insufficient stock → `AppError(400, 'OUT_OF_STOCK', '...')`
3. Compute `subtotal = sum(unitPrice * quantity)` — integers only
4. Apply discount: `type === 'percentage'` → `Math.floor(subtotal * value / 100)`; `type === 'fixed'` → `Math.min(value, subtotal)`
5. `total = subtotal - discountAmount + shippingAmount`
6. Generate reference: `'THIA-' + randomBytes(3).toString('hex').toUpperCase()`
7. Insert into `orders` table; insert all rows into `order_items` table
8. Return full order with items

**`GET /orders`** pagination: `?page=1&limit=10`, return `{ data: Order[], meta: { total, page, limit, totalPages } }`

**`GET /orders/:id`**: query by id AND `user_id = request.user.id`; throw `AppError(404)` if not found

**Registration**: `fastify.register(ordersRoutes)` in `src/app.ts`

## Dependencies
- Story 3.1 (Shipping Zones) — `shippingZoneId` and `shippingAmount` come from a prior `/shipping/calculate` call
- Story 3.2 (Discount Codes) — `discountCode` validated before order creation

## Definition of Done
- [x] All acceptance criteria met
- [x] Zero TypeScript errors
- [x] No `any` types
- [x] Out-of-stock items are rejected with a clear error message
- [x] Order reference matches `THIA-XXXXXX` pattern
- [x] `GET /orders/:id` returns 404 for another user's order
- [x] Pagination meta is correct on `GET /orders`
- [ ] Reviewed by QA

## Dev Agent Record

### File List
- `packages/shared/types/order.ts` — added `CreateOrderRequest` interface
- `apps/backend/src/routes/orders/index.ts` — created, implements `POST /orders`, `GET /orders`, `GET /orders/:id`
- `apps/backend/src/app.ts` — registered `ordersRoutes`

### Completion Notes
Stock validation queries `product_variants` with a product name join for clear error messages. The reference uses `randomBytes(3).toString('hex').toUpperCase()` producing exactly 6 hex chars. On order_items insert failure, best-effort rollback deletes the orphaned order row. Nested `products` join is handled with Array.isArray check since Supabase types it as an array. TypeScript check passes with exit 0.
