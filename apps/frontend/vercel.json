{
  "buildCommand": "cd ../.. && npm install && npm run build --workspace=packages/shared && npm run build --workspace=apps/frontend",
  "outputDirectory": ".output/public",
  "installCommand": "echo 'install handled in buildCommand'"
}
